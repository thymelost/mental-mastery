import 'package:flutter/material.dart';

// ─── Spacing ───────────────────────────────────────────────────────────────
const double spaceXs = 4.0;
const double spaceSm = 8.0;
const double spaceMd = 16.0;
const double spaceLg = 24.0;
const double spaceXl = 32.0;
const double spaceXxl = 48.0;

// ─── Radius ────────────────────────────────────────────────────────────────
const double radiusSm = 4.0;
const double radiusMd = 8.0;
const double radiusLg = 12.0;
const double radiusXl = 16.0;
const double radiusPill = 100.0;

// ─── Elevation ─────────────────────────────────────────────────────────────
const double elevationLow = 1.0;
const double elevationMid = 3.0;
const double elevationHigh = 6.0;

// ─── Colors ────────────────────────────────────────────────────────────────
const Color backgroundDark = Color(0xFF0D0F0F);
const Color surfaceDark = Color(0xFF1A1D1D);
const Color surfaceVariantDark = Color(0xFF252929);
const Color onSurfaceDark = Color(0xFFE8ECEC);
const Color onSurfaceMuted = Color(0xFF9EAAAA);

const Color primaryTeal = Color(0xFF4DB6AC);
const Color primaryTealDim = Color(0xFF37847C);

const Color errorRed = Color(0xFFCF4C4C);
const Color warningAmber = Color(0xFFE6A817);
const Color gatePassGreen = Color(0xFF4CAF7A);

const Color phaseLocked = Color(0xFF4A4A4A);
const Color phaseActive = primaryTeal;
const Color phaseComplete = gatePassGreen;

// ─── Timeline ──────────────────────────────────────────────────────────────
const double timelineLineWidth = 2.0;
const double timelineNodeRadius = 10.0;
const double timelineNodeRadiusActive = 12.0;
